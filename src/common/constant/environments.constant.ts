/**
 * redis url
 * @returns string
 */
export const ENV_REDIS_URL: string = 'REDIS_URL';

/**
 * redis password
 * @returns string
 */
export const ENV_REDIS_PASSWORD: string = 'REDIS_PASSWORD';

/**
 * access token secret
 * @returns string
 */
export const ENV_ACCESS_TOKEN_SECRET: string = 'ACCESS_TOKEN_SECRET';

/**
 * access token expires time
 * @returns string
 */
export const ENV_ACCESS_TOKEN_EXPIRES: string = 'ACCESS_TOKEN_EXPIRES';

/**
 * refresh token secret
 * @returns string
 */
export const ENV_REFRESH_TOKEN_SECRET: string = 'REFRESH_TOKEN_SECRET';

/**
 * refresh token expires time
 * @returns string
 */
export const ENV_REFRESH_TOKEN_EXPIRES: string = 'REFRESH_TOKEN_EXPIRES';
